(* This file contains useful functions that arguably (i.e., in comparison
 * with OCaml) belong in the standard library but have not been included
 * in it yet. It should eventually fall out of use.
 *
 * Nonetheless, as long as it hasn't, it should be placed in the same directory
 * as the generated parser. *)

pub let optionGet {~re : {type X} -> Unit ->[|_] X} opt =
    match opt with
    | None => ~re ()
    | Some x => x
    end
