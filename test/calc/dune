(rule
 (deps Parser.mly)
 (target Parser.fram)
 (action
  (run cpspg -o %{target} %{deps})))

(rule
  (alias runtest)
  (deps Calc.fram Parser.fram (source_tree ../../framtools))
  (action (run dbl -I ../../framtools Calc.fram)))
